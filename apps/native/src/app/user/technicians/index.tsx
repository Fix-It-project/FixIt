import NewTechnicians from "@/src/features/newtechnicians/NewTechnicians";

// Entry from a category: lists that category's technicians (search / sort /
// quick-profile sheet). Tapping a card opens the technician detail page.
export default function TechniciansScreen() {
	return <NewTechnicians />;
}
