// Role-scoped wrapper — the shared map picker handles everything and hands the
// chosen coords back via the picked-location store.
export { default } from "@/src/features/location-picker/components/MapLocationPicker";
