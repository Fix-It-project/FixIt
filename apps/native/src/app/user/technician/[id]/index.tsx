import NewTechnician from "@/src/features/newtechnician/NewTechnician";

// Technician detail: About / Services / Reviews. Service selection happens here
// (required before booking); "Select Date" carries the chosen service forward.
export default function TechnicianDetailScreen() {
	return <NewTechnician />;
}
